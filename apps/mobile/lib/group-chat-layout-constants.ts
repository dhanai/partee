/**
 * One rhythm for fullscreen group chat: same gap as between the TextInput and send button
 * (`composerRow` `gap`). Used for (1) space above the software keyboard, (2) scroll padding
 * below the last message — same pattern as Messenger / WhatsApp / Discord toolbars.
 */
export const GROUP_CHAT_COMPOSER_GAP = 8;
